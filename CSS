/* Define uma cor de fundo para o corpo da página */
body {
    background-color: #F4A460; /* Cor de fundo suave */
    
    justify-content: center;
    align-items: center;
    height: 100vh;
    margin: 0;
    
}

/* Centraliza o conteúdo dentro da div principal */
#main {
    text-align: center;
    background-color: #ffffff; /* Cor de fundo para o bloco principal */
    padding: 20px;
    
}

#test {
    padding: 15px
}

/* Estilo dos inputs */
input[type="number"] {
    display: block;
    width: 80%;
    margin: 10px auto;
    padding: 10px;
    border: 1px solid #ccc;
    border-radius: 5px;
    font-size: 16px;
}

/* Estilo do botão */
button {
    margin: 20px; /* Margem ao redor do botão */
    padding: 10px 20px; /* Espaçamento interno, topo e laterais */
    font-size: 16px;
    background-color: #4CAF50;
    color: white;
    border: none;
    cursor: pointer;
}

button:hover {
    background-color: #45a049; /* Altera o estilo do botão quando o usuário passa o cursor nele */
}


